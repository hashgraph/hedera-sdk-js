// import Hbar from "../src/Hbar";
// import newClient from "./IntegrationClient";
// import AccountBalanceQuery from "../src/account/AccountBalanceQuery";

describe("ClientIntegration", function () {
    it("should be executable", async function () {
        // this.timeout(10000);

        // let network = /** @type {Map<string, string>} */ new Map();
        // network.set("0.testnet.hedera.com:50211", 3);
        // network.set("0.testnet.hedera.com:50211", 4);

        // const client = newClient().setMaxQueryPayment(new Hbar(2));

        // const operatorId = client.getOperatorId();
        // expect(operatorId).to.not.be.null;

        // await new AccountBalanceQuery()
        //     .setAccountId(operatorId)
        //     .execute(client);

        // await new AccountBalanceQuery()
        //     .setAccountId(operatorId)
        //     .execute(client);

        // network = /** @type {Map<string, string>} */ new Map();
        // network.set("0.testnet.hedera.com:50211", 4);
        // network.set("0.testnet.hedera.com:50211", 5);

        // client._setNetwork(client);

        // network = /** @type {Map<string, string>} */ new Map();
        // network.set("35.186.191.247:50211", 4);
        // network.set("35.192.2.25:50211", 5);

        // client._setNetwork(client);
    });
});
